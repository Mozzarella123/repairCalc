import {combineReducers} from "redux";
import RoomEditorState from "./state";

const roomEditorReducers = combineReducers({
	// sub-stores
});

export const initialRoomEditorState : RoomEditorState = {}

export default roomEditorReducers;
